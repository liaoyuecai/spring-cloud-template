server:
  port: 7073


spring:
  application:
    name: zuul

eureka:
  client:
    service-url:
      default-zone: 127.0.0.1:8761/eureka/
zuul:
  routes:
    application:
      service-id: application
      path: /application/**

